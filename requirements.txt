# Lightweight AI Agent - Render Deployment Ready
# Optimized for cloud deployment without heavy AI models

# Core web framework
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Basic utilities - no heavy ML dependencies
requests==2.31.0
python-dotenv==1.0.1

# Free LLM capabilities (optional - graceful fallback if not available)
transformers==4.45.0
torch==2.5.1
sentencepiece==0.2.0

# Required for deployment stability
packaging==23.2