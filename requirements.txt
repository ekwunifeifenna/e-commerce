# Lightweight AI Agent - Render Free Tier Optimized
# Total memory footprint: ~100-150MB (well within 512MB limit)

# Core web framework
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Essential utilities only
requests==2.31.0
python-dotenv==1.0.1

# Lightweight text processing (instead of heavy LLM)
nltk==3.8.1

# Required for deployment stability
packaging==23.2

# Optional: Very lightweight ML (only if needed)
# scikit-learn==1.3.0  # Uncomment only if you need basic ML features